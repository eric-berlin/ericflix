var torrents = require('torrent-stream')
var fs = require('fs')

var engine = torrents(fs.readFileSync('test.torrent'), {
  path: process.cwd()//,
//  trackers: ['udp://registry.mathiasbuus.eu:9000']
})

engine.on('verify', function(index) {
  console.log('verified %d', index)
})

engine.on('ready', function() {
  console.log('ready...')
  engine.files.forEach(function(f) {

  })
})

engine.listen()